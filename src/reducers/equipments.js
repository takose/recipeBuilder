const initialState = {
  4: {
    name: 'ff',
    puttableToolIds: [0, 3],
  },
  5: {
    name: 'table',
    puttableToolIds: [1, 2],
  },
};

const equipments = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default equipments;

