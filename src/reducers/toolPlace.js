const initialState = {
  pot: 'ff',
  puta: 'table',
  knife: 'table',
  hera: 'ff',
  spoon: 'table',
};

const equipments = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default equipments;
